# CF-Log2JSON-Converter-Script
PowerShell script that converts Adobe ColdFusion .log files into structured # JSON (NDJSON) format for easier ingestion into logging platforms such as # Splunk, Datadog, ELK, or similar tools.
